<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/css/style.css">
    <title>Register</title>
</head>
<body>
    <%if (typeof(emptyFields) !== "undefined"){%>
        <div class="alert alert-danger" id = "alert-dismissible" role="alert">Required Fields Are Empty!</div>
    <%}%>
    <%if (typeof(signupError) !== "undefined"){%>
        <div class="alert alert-danger" id = "alert-dismissible" role="alert">Error Occured With Signup! This is an Issue on our side,
            our engineers are working tirelessly to make it better. You can contact us at <a href="">Contact Us</a>.
        </div>
    <%}%>
    <%if (typeof(userInputError) !== "undefined"){%>
        <div class="alert alert-danger" id = "alert-dismissible" role="alert">
            <span id="alert-dismiss">❌</span></br>
            <%userInputError.map((Error) => {%>
                <%=Error%></br>
            <%});%>
        </div>
    <%}%>
    <form action="/auth/register" method="post">
        <label for="username"><span class="requiredAsterik">*</span> Username:</label>
        <input type="text" name="username" id="username"></br>
        <label for="email"><span class="requiredAsterik">*</span> Email:</label>
        <input type="email" name="email" id="email"></br>
        <label for="password"><span class="requiredAsterik">*</span> Password:</label>
        <input type="password" name="password" id="password"></br>
        <input type="submit" value="submit">
    </form>
    <script src="/js/function.js"></script>
</body>
</html>